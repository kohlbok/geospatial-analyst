# Geospatial
geopandas>=0.14.0
shapely>=2.0.0
pyogrio>=0.9.0
pyproj>=3.6.0
overpy>=0.7

# Visualization
folium>=0.15.0
branca>=0.7.0
matplotlib>=3.8.0

# Data
pandas>=2.1.0
numpy>=1.25.0
scipy>=1.11.0
scikit-image>=0.21.0
scikit-learn>=1.3.0
openpyxl>=3.1.0
xlrd>=2.0.1
lxml>=5.0.0
orjson>=3.9.0
requests>=2.31.0

# Export
simplekml>=1.3.6
jinja2>=3.1.0

# PDF (optional). Requires GTK+ system libraries.
# macOS: brew install pango
# Windows: skipped automatically (HTML output is used as fallback). To enable, see
# https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
weasyprint>=60.0; sys_platform != "win32"

# MCP
mcp[cli]>=1.0.0
fastmcp>=0.1.0
